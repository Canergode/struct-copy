# Struct Copy

This project demonstrates how to define a struct, initialize its fields using strcpy and direct assignment, and copy one struct variable into another in C.

## What I Learned
- How to define a struct inside main
- How to initialize struct fields using strcpy and direct assignment
- How to copy an entire struct to another variable with a single assignment
- How struct copying works in C
